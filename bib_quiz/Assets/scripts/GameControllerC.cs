using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameControllerC : MonoBehaviour
{

    [Header("Configuração do cenario")]

    public float _cenarioDestruido;
    public float _cenarioTamanho;
    public GameObject _cenarioPrefab;
    public float _cenarioVelocidade;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
